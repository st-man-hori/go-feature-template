# Ports exposed on the host by compose.yaml
APP_PORT=8000
DB_PORT=3306

# Consumed directly by the app (no separate src/.env like the Laravel
# template — a Go binary has no framework-level .env split).
APP_ENV=local

DB_HOST=mysql
DB_DATABASE=app
DB_USERNAME=app
DB_PASSWORD=secret
DB_ROOT_PASSWORD=secret
